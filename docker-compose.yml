version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=357357357
      - POSTGRES_DB=thingy-green-db
